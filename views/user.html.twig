{% extends "template.html.twig" %}

{% block content %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">{{ user.getUserLogin }}</h3>
        </div>
        <div class="panel-body">
            <p>Prénom : {{ user.getUserFirstname }}</p>
            <p>Nom : {{ user.getUserLastname | upper }}</p>
            <p>Identifiant de connexion : {{ user.getUserLogin }}</p>
            <p>Email : {{ user.getUserEmail }}</p>
        </div>
    </div>
{% endblock %}